# Install

If this file exists, the node requires installation before use.
Read the steps below, run them (bash is the standard tool), then
`moss nodes install <path>` touches the `.installed` marker.

<!--
Fill in install steps here. Delete this file entirely if the node
needs no installation (then .installed is not required either).
-->
