# {name}

A MOSS cell. See `moss cells specification` for the development guide.

## Quick Start

```bash
moss cells run {name}                        # launch via CLI (name mode)
python main.py                # launch directly (debug)
```

## Environment

Describe dependencies and setup steps here.

## Directory

```
{name}/
  CELL.md         # manifest — metadata + model instruction
  README.md       # this file — human developer entry
  main.py         # entry point
  INSTALL.md      # (optional) presence means install required
  scripts/        # (optional) dev/debug scripts
```
