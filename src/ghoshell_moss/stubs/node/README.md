# {name}

<!--
Developer-facing entry (not shown to models at runtime).
Fill in when the node is worth maintaining or distributing standalone.

If this node is a git-clone-able repo of its own, you may also see:
pyproject.toml, uv.lock, tests/, etc. — all optional.
-->

## Usage

## Environment

## Notes
