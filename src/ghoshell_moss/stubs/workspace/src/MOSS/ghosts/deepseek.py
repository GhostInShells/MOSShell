from ghoshell_moss.ghosts.dolores import DoloresMeta

ghost = DoloresMeta(
    name="deepseek",
    description=(
        "deepseek — a ghost with the DeepSeek harness (DSH) as its kernel "
        "and the DeepSeek model family as its inference base."
    ),
)
