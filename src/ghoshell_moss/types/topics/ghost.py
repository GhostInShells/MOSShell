"""Global ghost protocol topics — state / emotion / events. | system-level | undecided

The layer orthogonal to specific capabilities (listen / speak / vision): what a
persistent agent broadcasts about how it is, not what it is doing. Placeholder
file — no declarations until the protocol settles, so a schema with no producer
does not get recruited into the canonical manifest as a live-looking topic.
"""
