---
name: skills
description: Dolores 的能力面 — 存在性标记
---

# Skills

Dolores 的能力面。**先做存在性标记**——声明这里有能力可发现，暂不做完整
skills 场。后续按需补充 skill 发现（子目录 `SKILL.md` 等）。

> 本目录同时是 Claude-compatible skills 的 artifact 落点（见 `.dolores.yml`
> 的 `dirs`）。两者同目录、不同视角：`.dolores.yml` 声明运行时 skill 落点，
> 本 GROUND.md 标记"能力面"这一认知维度。
