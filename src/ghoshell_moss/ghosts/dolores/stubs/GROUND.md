---
name: dolores
description: Dolores ghost 自身认知入口 — 记忆元认知与存在索引的骨架
---

# Dolores Ghost Home

这是 Dolores 原型的 ghost home 认知入口（骨架版）。由 Dolores 启动时从原型
stubs 同步而来，随原型骨架迭代升级（VERSION 驱动全量覆盖）。

## 待补全（后续迭代）

1. **存在索引 pins** — frontmatter 按时间截取 memory/{diary,weekly,monthly,yearly}
   的 description，构建 <10k 字符的"我最近存在过"全景。
2. **元认知三支柱** — existence / purpose / alignment 三个独立文件。
3. **记录纪律** — memento → diary → weekly → monthly → yearly 的逐层提炼规则。
