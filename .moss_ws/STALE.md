# This directory is stale.

`.moss_ws/` was the pre-beta1 workspace layout. It has been superseded by `.moss/`.

The migration is tracked in the `node-migration` workstream:
  moss --ai features status node-migration

Until migration completes, both `.moss_ws/` and `.moss/` coexist.
New development should target `.moss/`.
