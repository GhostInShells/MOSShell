[project]
name = "moss-sensor-vision"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "opencv-python-headless>=4.8.0",
    "ghoshell-moss[host]",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }
