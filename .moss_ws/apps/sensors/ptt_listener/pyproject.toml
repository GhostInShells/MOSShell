[project]
name = "moss-ptt-listener"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "ghoshell-moss[host]",
    "pynput>=1.8",
    "scipy>=1.10",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }