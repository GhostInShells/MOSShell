---
version: 1
completed: 2026-07-18
operator: claude-opus-4-7 (dogfood) + human architect
---

# Baseline 2026-07-18 v1

First complete dogfood after the `moss cells` → `moss nodes` rename +
path-based redesign. Round exposed a fossil `spawn_cwd` concept and
several kernel-adjacent drifts (project_id in MOSS.md, missing `modes create`,
M11-B default_mode). All corrected in the same commit.

## Results

| Case ID | Priority | Description | First Test | Fix | Final Result |
|---------|----------|-------------|------------|-----|--------------|
| TC-001  | P0 | `moss init -f` preserves project_id | FAIL | Commit `083f6688` moved project_id into `MossMeta` frontmatter (from originally intended sidecar file); `init -f` overwrites MOSS.md → wipes the id. Reverted to `workspace/project_id` sidecar (per WORKSPACE_PROJECT_ID_FILE, still declared in code); MossMeta.project_id field removed. | PASS |
| TC-002  | P0 | Stub MOSS.md dumped from MossMeta defaults | FAIL | Stub was hand-written with all-comment frontmatter → `frontmatter.load` yielded empty metadata; all fields silently defaulted from code. Regenerated stub from `MossMeta().model_dump(exclude={'file','system_project'})`. | PASS |
| TC-003  | P0 | M11-B default_mode fallback → `default` | FAIL | `MossMeta.default_mode` Field default was `NONE_MOSS_MODE` ("none"). Landed M11-B: added `DEFAULT_MODE_NAME = "default"` constant, changed Field default. `NONE_MOSS_MODE` retained for explicit opt-out. 10+ downstream usages untouched. | PASS |
| TC-004  | P1 | `moss modes create <name>` | BLOCKED | Command did not exist in `modes_cli.py` (only `list`/`show`). CLAUDE.md described `modes create` but code had drifted. Added minimal command: `moss modes create <name>` copies from `MODE_STUB_PACKAGE` to `project.get_mode_home(name)`, prints "Next: edit HOST.md". No `--node-paths` or other flags — user edits HOST.md directly. | PASS |
| TC-005  | P0 | `moss nodes list` mode-aware | PASS | — | PASS |
| TC-006  | P0 | `moss nodes create <path>` | PASS | — | PASS |
| TC-007  | P1 | `create` prints INSTALL.md hint | FAIL | Original `create` output only mentioned README.md, NODE.md, `run`. Added conditional INSTALL.md guidance (stub always ships INSTALL.md; hint only prints if file exists). | PASS |
| TC-008  | P0 | `moss nodes show` prints NODE.md verbatim + install warning | PARTIAL | `show` printed NODE.md but had no install-status guidance. Added post-panel check: if `INSTALL.md` exists and `.installed` doesn't → `[WARN] Not installed...` + Read/Then hints. | PASS |
| TC-009  | P0 | `moss nodes install` touches `.installed` | PASS | Uses `NodeManifest.INSTALLED_FILE` constant (not magic string). No changes. | PASS |
| TC-010  | P0 | `run` uses `manifest.cwd` | FAIL | Fossil `spawn_cwd = env.cell_runtimes_dir / normalize(address)` in `run` (L410) — CLI-invented concept absent from blueprint. Fed to `subprocess.Popen(cwd=...)` → child could not find `main.py` (which lived in the NODE.md's dir, not the runtime dir). Also produced orphan empty directories (`spawn_cwd.mkdir(parents=True, exist_ok=True)` unconditional). Fix: deleted all `spawn_cwd` references (L410, L411, L413, L425); Popen now uses `launcher.cwd` directly (blueprint contract). | PASS |
| TC-011  | P0 | `run` env debug leaks no secrets | FAIL — CRITICAL | `_print_launch_debug` iterated `launcher.env.keys()` filtering `key.startswith("MOSS_")`. This leaked `MOSS_LLM_API_KEY`, `MOSS_LLM_BASE_URL`, `MOSS_LLM_MODEL` (all MOSS_-prefixed but not runtime scope). Rotated key value immediately. Fix: use `env.dump_runtime_scope()` (returns closed set of 9 MOSS runtime scope keys); the `launcher.env` param removed from `_print_launch_debug`. | PASS |
| TC-012  | P1 | `run` refuses uninstalled node | PASS | — | PASS |
| TC-013  | P1 | Singleton lock refuses second instance | PASS (semantic improvement) | Original code did a pre-check by scanning all runtimes AND acquired the lock — pre-check had TOCTOU race and duplicated the lock's authority. Simplified: only acquire `env.workspace.lock(locker_name())`; on failure print friendly `Singleton conflict for '...'` with next-step hints (status/kill). | PASS |
| TC-014  | P0 | `status` uid prefix match | FAIL | `status` used `info.address == address or info.address.endswith(address)`. `endswith` is string-level not segment-level — a query of `01` would match any uid ending in `01`; a query like `hello_world/01KX...` would also match. Introduced `_match_address(info_address, query)`: exact address OR `parse_address(...)` uid segment starts-with query (git short-hash style). | PASS |
| TC-015  | P0 | `status` detail has no `spawn_cwd` row | FAIL | Detail table had a `spawn_cwd` row (fossil, duplicated `cell.home` which was already displayed). Row removed. Also removed the unused `normalize` import. | PASS |
| TC-016  | P1 | Ambiguous query rejected clearly | FAIL | Original `status`/`kill` did `break` on first match — silently picked the first alphabetical hit. Added `_resolve_single_runtime(runtime_dir, query, action=...)`: collects all matches, errors with all addresses listed when > 1. | PASS |
| TC-017  | P0 | `kill --force` sends SIGKILL, ledger removed, no orphan | PASS | Killed 01KXTP8G (pid 18330) cleanly; runtime dir has only README/gitignore afterwards; no orphan in `ps -ef`. | PASS |
| TC-018  | P1 | Crash-fast emits semantic error | FAIL | `run` had `except Exception as e: print_error(...) raise typer.Exit(1)` — swallowed traceback and left operator with just child stderr and no status summary. Contracted the except to only shield the launch path (typer.Exit passthrough); added post-wait check: `if proc.returncode != 0: print_error(f"Node exited abnormally (returncode={returncode}). See child stderr above for cause.")`. | PASS |
| TC-019  | P1 | `prune` cleans stale + kills alive orphans | PASS (untouched surface) | — | PASS |
| TC-020  | P2 | `prune --keep-alive` preserves live orphans | PASS | — | PASS |
| TC-021  | P2 | `link` shortcut with `--command` | PASS | — | PASS |
| TC-022  | P2 | `link` without `--command` fails helpfully | PASS | — | PASS |

## Summary

- **Total cases**: 22
- **First-test failures**: 12 (P0: 8, P1: 3, P2: 1)
- **Final PASS**: 22 / 22
- **Root cause classes**:
  - Kernel drift from earlier commits (project_id, stub generation, default_mode fallback) — 3 cases
  - Fossil concept in CLI (`spawn_cwd`) — 3 cases (run + status + orphan dir)
  - Contract-bypass in CLI (hand-rolled env filter → leak; hand-rolled address match → ambiguity) — 3 cases
  - UX gaps in CLI (install guidance in create/show; ambiguity error; crash-fast summary) — 3 cases

## Sibling artifacts committed same round

- Kernel: `environment.py` (project_id sidecar restored; MossMeta.project_id removed; `DEFAULT_MODE_NAME` added; `default_mode` Field default changed).
- Stubs: `stubs/workspace/MOSS.md` and `stubs/workspace/modes/default/HOST.md` regenerated from `MossMeta().model_dump()` / `HostModeMeta().model_dump()`.
- CLI: `moss modes create` added; `nodes_cli.py` reviewed end-to-end and rewritten per audit; top docstring updated with "thin-shell future direction" note.
- Workspace: `.moss/cells/` deleted (rename residue); `.moss/nodes/` added (fresh stub sync); `.moss/MOSS.md` and `.moss/modes/default/HOST.md` synced from updated stub.

## What This Baseline Establishes

- The `moss nodes` CLI's 9-command surface behaves per §R-1 / §R-2 judgment
  in `cells-cli` FEATURE.md.
- All CLI-side actions delegate to blueprint abstractions
  (`NodeLauncher.cwd`, `env.dump_runtime_scope`, `env.workspace.lock`,
  `CellRuntimeInfo.iter_runtime_info`, `_match_address` via `parse_address`).
- `moss init -f` is safe for stub upgrade — it does not destroy workspace
  identity or user-edited configuration in MOSS.md.
- `moss modes create` exists again (was missing entirely).
- The regression set can be re-run against future commits to guard the
  contract lines this round drew.
