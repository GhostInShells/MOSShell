# dsh 独立 DSH_HOME — 启动声明保留, 可重建产物全 ignore.
# 保留: profiles/web/*.yml + package.json (cordis 配置, 启动必需).
# ignore: 运行时生成/可重建的一切.

# 运行时生成
settings.yaml
.anonymous-user-id

# 会话与状态 (运行时产物, 可重建)
sessions/
storages/

# 依赖安装 (pnpm install 可重建)
node_modules/
profiles/node_modules/
profiles/*/node_modules/
