---
name: "adder_py"
description: "A simple python-mode app (loads main.py as a script)."
main: "main.py"
mode: "python"
---

## Usage

- Command: `add(a, b) -> int`
- Command: `hello(name) -> str`

This app is launched in `mode=python`, meaning the app's `main` is treated as a python file.
