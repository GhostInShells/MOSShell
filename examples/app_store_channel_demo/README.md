# AppStoreChannel Demo

This demo scans the repo-level `apps/` directory and manages three apps:

- `adder_py` (`mode=python`)
- `math_mod` (`mode=module`)
- `explicit_chan` (`mode=channel`)

Run:

```bash
python examples/app_store_channel_demo/main.py
```
